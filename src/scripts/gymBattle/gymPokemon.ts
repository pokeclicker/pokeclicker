class gymPokemon{
    name: string;
    maxHealth : number;
    level : number;


    constructor(name: string, maxHealth: number, level: number) {
        this.name = name;
        this.maxHealth = maxHealth;
        this.level = level;
    }
}
