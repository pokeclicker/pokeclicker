interface MutationReqInterface {
    berryType: BerryType;       // Type of Berry for the rqeuirement
    berryStage: PlotStage;      // Stage of growth the berry must be in. Set to PlotStage.Seed if no requirement
}