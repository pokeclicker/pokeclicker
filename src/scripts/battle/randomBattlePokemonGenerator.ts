class randomBattlePokemonGenerator  {

    public static generate(route: number, ): enemyPokemonInterface {
        if (route > 25){
            return null;
        }


        let name: string;
        let type1: PokemonTypes;
        let type2: PokemonTypes;
        let maxHealth: number;
        let catchRate: number;
        let exp: number;
        let money: number;
    }

}